// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

import { CycleLearnSkillSystem } from "../../CycleLearnSkillSystem.sol";
import { revertWithBytes } from "@latticexyz/world/src/revertWithBytes.sol";
import { IWorldCall } from "@latticexyz/world/src/IWorldKernel.sol";
import { SystemCall } from "@latticexyz/world/src/SystemCall.sol";
import { WorldContextConsumerLib } from "@latticexyz/world/src/WorldContext.sol";
import { Systems } from "@latticexyz/world/src/codegen/tables/Systems.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";

type CycleLearnSkillSystemType is bytes32;

// equivalent to WorldResourceIdLib.encode({ typeId: RESOURCE_SYSTEM, namespace: "cycle", name: "CycleLearnSkillS" }))
CycleLearnSkillSystemType constant cycleLearnSkillSystem = CycleLearnSkillSystemType.wrap(
  0x73796379636c650000000000000000004379636c654c6561726e536b696c6c53
);

struct CallWrapper {
  ResourceId systemId;
  address from;
}

struct RootCallWrapper {
  ResourceId systemId;
  address from;
}

/**
 * @title CycleLearnSkillSystemLib
 * @author MUD (https://mud.dev) by Lattice (https://lattice.xyz)
 * @dev This library is automatically generated from the corresponding system contract. Do not edit manually.
 */
library CycleLearnSkillSystemLib {
  error CycleLearnSkillSystemLib_CallingFromRootSystem();
  error CycleLearnSkillSystem_SkillNotInGuiseSkills(bytes32 skillEntity, bytes32 guiseEntity);
  error CycleLearnSkillSystem_LevelIsTooLow(uint32 currentLevel, uint32 requiredLevel);

  function learnSkill(CycleLearnSkillSystemType self, bytes32 cycleEntity, bytes32 skillEntity) internal {
    return CallWrapper(self.toResourceId(), address(0)).learnSkill(cycleEntity, skillEntity);
  }

  function learnSkill(CallWrapper memory self, bytes32 cycleEntity, bytes32 skillEntity) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert CycleLearnSkillSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_learnSkill_bytes32_bytes32.learnSkill, (cycleEntity, skillEntity));
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function learnSkill(RootCallWrapper memory self, bytes32 cycleEntity, bytes32 skillEntity) internal {
    bytes memory systemCall = abi.encodeCall(_learnSkill_bytes32_bytes32.learnSkill, (cycleEntity, skillEntity));
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function callFrom(CycleLearnSkillSystemType self, address from) internal pure returns (CallWrapper memory) {
    return CallWrapper(self.toResourceId(), from);
  }

  function callAsRoot(CycleLearnSkillSystemType self) internal view returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), WorldContextConsumerLib._msgSender());
  }

  function callAsRootFrom(CycleLearnSkillSystemType self, address from) internal pure returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), from);
  }

  function toResourceId(CycleLearnSkillSystemType self) internal pure returns (ResourceId) {
    return ResourceId.wrap(CycleLearnSkillSystemType.unwrap(self));
  }

  function fromResourceId(ResourceId resourceId) internal pure returns (CycleLearnSkillSystemType) {
    return CycleLearnSkillSystemType.wrap(resourceId.unwrap());
  }

  function getAddress(CycleLearnSkillSystemType self) internal view returns (address) {
    return Systems.getSystem(self.toResourceId());
  }

  function _world() private view returns (IWorldCall) {
    return IWorldCall(StoreSwitch.getStoreAddress());
  }
}

/**
 * System Function Interfaces
 *
 * We generate an interface for each system function, which is then used for encoding system calls.
 * This is necessary to handle function overloading correctly (which abi.encodeCall cannot).
 *
 * Each interface is uniquely named based on the function name and parameters to prevent collisions.
 */

interface _learnSkill_bytes32_bytes32 {
  function learnSkill(bytes32 cycleEntity, bytes32 skillEntity) external;
}

using CycleLearnSkillSystemLib for CycleLearnSkillSystemType global;
using CycleLearnSkillSystemLib for CallWrapper global;
using CycleLearnSkillSystemLib for RootCallWrapper global;

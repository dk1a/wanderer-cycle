// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

import { StartCycleSystem } from "../../cycle/StartCycleSystem.sol";
import { revertWithBytes } from "@latticexyz/world/src/revertWithBytes.sol";
import { IWorldCall } from "@latticexyz/world/src/IWorldKernel.sol";
import { SystemCall } from "@latticexyz/world/src/SystemCall.sol";
import { WorldContextConsumerLib } from "@latticexyz/world/src/WorldContext.sol";
import { Systems } from "@latticexyz/world/src/codegen/tables/Systems.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";

type StartCycleSystemType is bytes32;

// equivalent to WorldResourceIdLib.encode({ typeId: RESOURCE_SYSTEM, namespace: "", name: "StartCycleSystem" }))
StartCycleSystemType constant startCycleSystem = StartCycleSystemType.wrap(
  0x7379000000000000000000000000000053746172744379636c6553797374656d
);

struct CallWrapper {
  ResourceId systemId;
  address from;
}

struct RootCallWrapper {
  ResourceId systemId;
  address from;
}

/**
 * @title StartCycleSystemLib
 * @author MUD (https://mud.dev) by Lattice (https://lattice.xyz)
 * @dev This library is automatically generated from the corresponding system contract. Do not edit manually.
 */
library StartCycleSystemLib {
  error StartCycleSystemLib_CallingFromRootSystem();

  function startCycle(
    StartCycleSystemType self,
    bytes32 wandererEntity,
    bytes32 guiseEntity,
    bytes32 wheelEntity
  ) internal returns (bytes32 cycleEntity) {
    return CallWrapper(self.toResourceId(), address(0)).startCycle(wandererEntity, guiseEntity, wheelEntity);
  }

  function startCycle(
    CallWrapper memory self,
    bytes32 wandererEntity,
    bytes32 guiseEntity,
    bytes32 wheelEntity
  ) internal returns (bytes32 cycleEntity) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert StartCycleSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _startCycle_bytes32_bytes32_bytes32.startCycle,
      (wandererEntity, guiseEntity, wheelEntity)
    );

    bytes memory result = self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
    return abi.decode(result, (bytes32));
  }

  function startCycle(
    RootCallWrapper memory self,
    bytes32 wandererEntity,
    bytes32 guiseEntity,
    bytes32 wheelEntity
  ) internal returns (bytes32 cycleEntity) {
    bytes memory systemCall = abi.encodeCall(
      _startCycle_bytes32_bytes32_bytes32.startCycle,
      (wandererEntity, guiseEntity, wheelEntity)
    );

    bytes memory result = SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
    return abi.decode(result, (bytes32));
  }

  function callFrom(StartCycleSystemType self, address from) internal pure returns (CallWrapper memory) {
    return CallWrapper(self.toResourceId(), from);
  }

  function callAsRoot(StartCycleSystemType self) internal view returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), WorldContextConsumerLib._msgSender());
  }

  function callAsRootFrom(StartCycleSystemType self, address from) internal pure returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), from);
  }

  function toResourceId(StartCycleSystemType self) internal pure returns (ResourceId) {
    return ResourceId.wrap(StartCycleSystemType.unwrap(self));
  }

  function fromResourceId(ResourceId resourceId) internal pure returns (StartCycleSystemType) {
    return StartCycleSystemType.wrap(resourceId.unwrap());
  }

  function getAddress(StartCycleSystemType self) internal view returns (address) {
    return Systems.getSystem(self.toResourceId());
  }

  function _world() private view returns (IWorldCall) {
    return IWorldCall(StoreSwitch.getStoreAddress());
  }
}

/**
 * System Function Interfaces
 *
 * We generate an interface for each system function, which is then used for encoding system calls.
 * This is necessary to handle function overloading correctly (which abi.encodeCall cannot).
 *
 * Each interface is uniquely named based on the function name and parameters to prevent collisions.
 */

interface _startCycle_bytes32_bytes32_bytes32 {
  function startCycle(bytes32 wandererEntity, bytes32 guiseEntity, bytes32 wheelEntity) external;
}

using StartCycleSystemLib for StartCycleSystemType global;
using StartCycleSystemLib for CallWrapper global;
using StartCycleSystemLib for RootCallWrapper global;

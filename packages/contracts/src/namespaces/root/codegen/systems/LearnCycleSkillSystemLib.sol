// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

import { LearnCycleSkillSystem } from "../../cycle/LearnCycleSkillSystem.sol";
import { revertWithBytes } from "@latticexyz/world/src/revertWithBytes.sol";
import { IWorldCall } from "@latticexyz/world/src/IWorldKernel.sol";
import { SystemCall } from "@latticexyz/world/src/SystemCall.sol";
import { WorldContextConsumerLib } from "@latticexyz/world/src/WorldContext.sol";
import { Systems } from "@latticexyz/world/src/codegen/tables/Systems.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";

type LearnCycleSkillSystemType is bytes32;

// equivalent to WorldResourceIdLib.encode({ typeId: RESOURCE_SYSTEM, namespace: "", name: "LearnCycleSkillS" }))
LearnCycleSkillSystemType constant learnCycleSkillSystem = LearnCycleSkillSystemType.wrap(
  0x737900000000000000000000000000004c6561726e4379636c65536b696c6c53
);

struct CallWrapper {
  ResourceId systemId;
  address from;
}

struct RootCallWrapper {
  ResourceId systemId;
  address from;
}

/**
 * @title LearnCycleSkillSystemLib
 * @author MUD (https://mud.dev) by Lattice (https://lattice.xyz)
 * @dev This library is automatically generated from the corresponding system contract. Do not edit manually.
 */
library LearnCycleSkillSystemLib {
  error LearnCycleSkillSystemLib_CallingFromRootSystem();
  error LearnCycleSkillSystem_SkillNotInGuiseSkills(bytes32 skillEntity, bytes32 guiseEntity);
  error LearnCycleSkillSystem_LevelIsTooLow(uint32 currentLevel, uint32 requiredLevel);

  function learnFromCycle(LearnCycleSkillSystemType self, bytes32 wandererEntity, bytes32 skillEntity) internal {
    return CallWrapper(self.toResourceId(), address(0)).learnFromCycle(wandererEntity, skillEntity);
  }

  function learnFromCycle(CallWrapper memory self, bytes32 wandererEntity, bytes32 skillEntity) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert LearnCycleSkillSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _learnFromCycle_bytes32_bytes32.learnFromCycle,
      (wandererEntity, skillEntity)
    );
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function learnFromCycle(RootCallWrapper memory self, bytes32 wandererEntity, bytes32 skillEntity) internal {
    bytes memory systemCall = abi.encodeCall(
      _learnFromCycle_bytes32_bytes32.learnFromCycle,
      (wandererEntity, skillEntity)
    );
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function callFrom(LearnCycleSkillSystemType self, address from) internal pure returns (CallWrapper memory) {
    return CallWrapper(self.toResourceId(), from);
  }

  function callAsRoot(LearnCycleSkillSystemType self) internal view returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), WorldContextConsumerLib._msgSender());
  }

  function callAsRootFrom(LearnCycleSkillSystemType self, address from) internal pure returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), from);
  }

  function toResourceId(LearnCycleSkillSystemType self) internal pure returns (ResourceId) {
    return ResourceId.wrap(LearnCycleSkillSystemType.unwrap(self));
  }

  function fromResourceId(ResourceId resourceId) internal pure returns (LearnCycleSkillSystemType) {
    return LearnCycleSkillSystemType.wrap(resourceId.unwrap());
  }

  function getAddress(LearnCycleSkillSystemType self) internal view returns (address) {
    return Systems.getSystem(self.toResourceId());
  }

  function _world() private view returns (IWorldCall) {
    return IWorldCall(StoreSwitch.getStoreAddress());
  }
}

/**
 * System Function Interfaces
 *
 * We generate an interface for each system function, which is then used for encoding system calls.
 * This is necessary to handle function overloading correctly (which abi.encodeCall cannot).
 *
 * Each interface is uniquely named based on the function name and parameters to prevent collisions.
 */

interface _learnFromCycle_bytes32_bytes32 {
  function learnFromCycle(bytes32 wandererEntity, bytes32 skillEntity) external;
}

using LearnCycleSkillSystemLib for LearnCycleSkillSystemType global;
using LearnCycleSkillSystemLib for CallWrapper global;
using LearnCycleSkillSystemLib for RootCallWrapper global;

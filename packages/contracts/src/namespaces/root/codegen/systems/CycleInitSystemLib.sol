// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

import { CycleInitSystem } from "../../cycle/CycleInitSystem.sol";
import { revertWithBytes } from "@latticexyz/world/src/revertWithBytes.sol";
import { IWorldCall } from "@latticexyz/world/src/IWorldKernel.sol";
import { SystemCall } from "@latticexyz/world/src/SystemCall.sol";
import { WorldContextConsumerLib } from "@latticexyz/world/src/WorldContext.sol";
import { Systems } from "@latticexyz/world/src/codegen/tables/Systems.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";

type CycleInitSystemType is bytes32;

// equivalent to WorldResourceIdLib.encode({ typeId: RESOURCE_SYSTEM, namespace: "", name: "CycleInitSystem" }))
CycleInitSystemType constant cycleInitSystem = CycleInitSystemType.wrap(
  0x737900000000000000000000000000004379636c65496e697453797374656d00
);

struct CallWrapper {
  ResourceId systemId;
  address from;
}

struct RootCallWrapper {
  ResourceId systemId;
  address from;
}

/**
 * @title CycleInitSystemLib
 * @author MUD (https://mud.dev) by Lattice (https://lattice.xyz)
 * @dev This library is automatically generated from the corresponding system contract. Do not edit manually.
 */
library CycleInitSystemLib {
  error CycleInitSystemLib_CallingFromRootSystem();

  function initCycle(
    CycleInitSystemType self,
    bytes32 wandererEntity,
    bytes32 cycleEntity,
    bytes32 guiseProtoEntity,
    bytes32 wheelEntity
  ) internal {
    return
      CallWrapper(self.toResourceId(), address(0)).initCycle(
        wandererEntity,
        cycleEntity,
        guiseProtoEntity,
        wheelEntity
      );
  }

  function initCycle(
    CallWrapper memory self,
    bytes32 wandererEntity,
    bytes32 cycleEntity,
    bytes32 guiseProtoEntity,
    bytes32 wheelEntity
  ) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert CycleInitSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _initCycle_bytes32_bytes32_bytes32_bytes32.initCycle,
      (wandererEntity, cycleEntity, guiseProtoEntity, wheelEntity)
    );
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function initCycle(
    RootCallWrapper memory self,
    bytes32 wandererEntity,
    bytes32 cycleEntity,
    bytes32 guiseProtoEntity,
    bytes32 wheelEntity
  ) internal {
    bytes memory systemCall = abi.encodeCall(
      _initCycle_bytes32_bytes32_bytes32_bytes32.initCycle,
      (wandererEntity, cycleEntity, guiseProtoEntity, wheelEntity)
    );
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function callFrom(CycleInitSystemType self, address from) internal pure returns (CallWrapper memory) {
    return CallWrapper(self.toResourceId(), from);
  }

  function callAsRoot(CycleInitSystemType self) internal view returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), WorldContextConsumerLib._msgSender());
  }

  function callAsRootFrom(CycleInitSystemType self, address from) internal pure returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), from);
  }

  function toResourceId(CycleInitSystemType self) internal pure returns (ResourceId) {
    return ResourceId.wrap(CycleInitSystemType.unwrap(self));
  }

  function fromResourceId(ResourceId resourceId) internal pure returns (CycleInitSystemType) {
    return CycleInitSystemType.wrap(resourceId.unwrap());
  }

  function getAddress(CycleInitSystemType self) internal view returns (address) {
    return Systems.getSystem(self.toResourceId());
  }

  function _world() private view returns (IWorldCall) {
    return IWorldCall(StoreSwitch.getStoreAddress());
  }
}

/**
 * System Function Interfaces
 *
 * We generate an interface for each system function, which is then used for encoding system calls.
 * This is necessary to handle function overloading correctly (which abi.encodeCall cannot).
 *
 * Each interface is uniquely named based on the function name and parameters to prevent collisions.
 */

interface _initCycle_bytes32_bytes32_bytes32_bytes32 {
  function initCycle(
    bytes32 wandererEntity,
    bytes32 cycleEntity,
    bytes32 guiseProtoEntity,
    bytes32 wheelEntity
  ) external;
}

using CycleInitSystemLib for CycleInitSystemType global;
using CycleInitSystemLib for CallWrapper global;
using CycleInitSystemLib for RootCallWrapper global;

// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

import { CycleCombatSystem } from "../../cycle/CycleCombatSystem.sol";
import { CombatAction } from "../../../../CustomTypes.sol";
import { CombatResult } from "../../../../codegen/common.sol";
import { revertWithBytes } from "@latticexyz/world/src/revertWithBytes.sol";
import { IWorldCall } from "@latticexyz/world/src/IWorldKernel.sol";
import { SystemCall } from "@latticexyz/world/src/SystemCall.sol";
import { WorldContextConsumerLib } from "@latticexyz/world/src/WorldContext.sol";
import { Systems } from "@latticexyz/world/src/codegen/tables/Systems.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";

type CycleCombatSystemType is bytes32;

// equivalent to WorldResourceIdLib.encode({ typeId: RESOURCE_SYSTEM, namespace: "", name: "CycleCombatSyste" }))
CycleCombatSystemType constant cycleCombatSystem = CycleCombatSystemType.wrap(
  0x737900000000000000000000000000004379636c65436f6d6261745379737465
);

struct CallWrapper {
  ResourceId systemId;
  address from;
}

struct RootCallWrapper {
  ResourceId systemId;
  address from;
}

/**
 * @title CycleCombatSystemLib
 * @author MUD (https://mud.dev) by Lattice (https://lattice.xyz)
 * @dev This library is automatically generated from the corresponding system contract. Do not edit manually.
 */
library CycleCombatSystemLib {
  error CycleCombatSystemLib_CallingFromRootSystem();

  function processCycleCombatRound(
    CycleCombatSystemType self,
    bytes32 wandererEntity,
    CombatAction[] memory initiatorActions
  ) internal returns (CombatResult result) {
    return CallWrapper(self.toResourceId(), address(0)).processCycleCombatRound(wandererEntity, initiatorActions);
  }

  function processCycleCombatRound(
    CallWrapper memory self,
    bytes32 wandererEntity,
    CombatAction[] memory initiatorActions
  ) internal returns (CombatResult result) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert CycleCombatSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _processCycleCombatRound_bytes32_CombatActionArray.processCycleCombatRound,
      (wandererEntity, initiatorActions)
    );

    bytes memory result = self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
    return abi.decode(result, (CombatResult));
  }

  function processCycleCombatRound(
    RootCallWrapper memory self,
    bytes32 wandererEntity,
    CombatAction[] memory initiatorActions
  ) internal returns (CombatResult result) {
    bytes memory systemCall = abi.encodeCall(
      _processCycleCombatRound_bytes32_CombatActionArray.processCycleCombatRound,
      (wandererEntity, initiatorActions)
    );

    bytes memory result = SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
    return abi.decode(result, (CombatResult));
  }

  function callFrom(CycleCombatSystemType self, address from) internal pure returns (CallWrapper memory) {
    return CallWrapper(self.toResourceId(), from);
  }

  function callAsRoot(CycleCombatSystemType self) internal view returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), WorldContextConsumerLib._msgSender());
  }

  function callAsRootFrom(CycleCombatSystemType self, address from) internal pure returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), from);
  }

  function toResourceId(CycleCombatSystemType self) internal pure returns (ResourceId) {
    return ResourceId.wrap(CycleCombatSystemType.unwrap(self));
  }

  function fromResourceId(ResourceId resourceId) internal pure returns (CycleCombatSystemType) {
    return CycleCombatSystemType.wrap(resourceId.unwrap());
  }

  function getAddress(CycleCombatSystemType self) internal view returns (address) {
    return Systems.getSystem(self.toResourceId());
  }

  function _world() private view returns (IWorldCall) {
    return IWorldCall(StoreSwitch.getStoreAddress());
  }
}

/**
 * System Function Interfaces
 *
 * We generate an interface for each system function, which is then used for encoding system calls.
 * This is necessary to handle function overloading correctly (which abi.encodeCall cannot).
 *
 * Each interface is uniquely named based on the function name and parameters to prevent collisions.
 */

interface _processCycleCombatRound_bytes32_CombatActionArray {
  function processCycleCombatRound(bytes32 wandererEntity, CombatAction[] memory initiatorActions) external;
}

using CycleCombatSystemLib for CycleCombatSystemType global;
using CycleCombatSystemLib for CallWrapper global;
using CycleCombatSystemLib for RootCallWrapper global;

// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

import { CompleteCycleSystem } from "../../cycle/CompleteCycleSystem.sol";
import { revertWithBytes } from "@latticexyz/world/src/revertWithBytes.sol";
import { IWorldCall } from "@latticexyz/world/src/IWorldKernel.sol";
import { SystemCall } from "@latticexyz/world/src/SystemCall.sol";
import { WorldContextConsumerLib } from "@latticexyz/world/src/WorldContext.sol";
import { Systems } from "@latticexyz/world/src/codegen/tables/Systems.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";

type CompleteCycleSystemType is bytes32;

// equivalent to WorldResourceIdLib.encode({ typeId: RESOURCE_SYSTEM, namespace: "", name: "CompleteCycleSys" }))
CompleteCycleSystemType constant completeCycleSystem = CompleteCycleSystemType.wrap(
  0x73790000000000000000000000000000436f6d706c6574654379636c65537973
);

struct CallWrapper {
  ResourceId systemId;
  address from;
}

struct RootCallWrapper {
  ResourceId systemId;
  address from;
}

/**
 * @title CompleteCycleSystemLib
 * @author MUD (https://mud.dev) by Lattice (https://lattice.xyz)
 * @dev This library is automatically generated from the corresponding system contract. Do not edit manually.
 */
library CompleteCycleSystemLib {
  error CompleteCycleSystemLib_CallingFromRootSystem();
  error CompleteCycleSystem__NotAllBossesDefeated();
  error CompleteCycleSystem__InsufficientLevel();

  function complete(CompleteCycleSystemType self, bytes memory args) internal returns (bytes memory) {
    return CallWrapper(self.toResourceId(), address(0)).complete(args);
  }

  function complete(CallWrapper memory self, bytes memory args) internal returns (bytes memory) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert CompleteCycleSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_complete_bytes.complete, (args));

    bytes memory result = self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
    return abi.decode(result, (bytes));
  }

  function complete(RootCallWrapper memory self, bytes memory args) internal returns (bytes memory) {
    bytes memory systemCall = abi.encodeCall(_complete_bytes.complete, (args));

    bytes memory result = SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
    return abi.decode(result, (bytes));
  }

  function callFrom(CompleteCycleSystemType self, address from) internal pure returns (CallWrapper memory) {
    return CallWrapper(self.toResourceId(), from);
  }

  function callAsRoot(CompleteCycleSystemType self) internal view returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), WorldContextConsumerLib._msgSender());
  }

  function callAsRootFrom(CompleteCycleSystemType self, address from) internal pure returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), from);
  }

  function toResourceId(CompleteCycleSystemType self) internal pure returns (ResourceId) {
    return ResourceId.wrap(CompleteCycleSystemType.unwrap(self));
  }

  function fromResourceId(ResourceId resourceId) internal pure returns (CompleteCycleSystemType) {
    return CompleteCycleSystemType.wrap(resourceId.unwrap());
  }

  function getAddress(CompleteCycleSystemType self) internal view returns (address) {
    return Systems.getSystem(self.toResourceId());
  }

  function _world() private view returns (IWorldCall) {
    return IWorldCall(StoreSwitch.getStoreAddress());
  }
}

/**
 * System Function Interfaces
 *
 * We generate an interface for each system function, which is then used for encoding system calls.
 * This is necessary to handle function overloading correctly (which abi.encodeCall cannot).
 *
 * Each interface is uniquely named based on the function name and parameters to prevent collisions.
 */

interface _complete_bytes {
  function complete(bytes memory args) external;
}

using CompleteCycleSystemLib for CompleteCycleSystemType global;
using CompleteCycleSystemLib for CallWrapper global;
using CompleteCycleSystemLib for RootCallWrapper global;

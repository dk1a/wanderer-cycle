// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

import { CycleNoncombatSkillSystem } from "../../cycle/CycleNoncombatSkillSystem.sol";
import { revertWithBytes } from "@latticexyz/world/src/revertWithBytes.sol";
import { IWorldCall } from "@latticexyz/world/src/IWorldKernel.sol";
import { SystemCall } from "@latticexyz/world/src/SystemCall.sol";
import { WorldContextConsumerLib } from "@latticexyz/world/src/WorldContext.sol";
import { Systems } from "@latticexyz/world/src/codegen/tables/Systems.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";

type CycleNoncombatSkillSystemType is bytes32;

// equivalent to WorldResourceIdLib.encode({ typeId: RESOURCE_SYSTEM, namespace: "", name: "CycleNoncombatSk" }))
CycleNoncombatSkillSystemType constant cycleNoncombatSkillSystem = CycleNoncombatSkillSystemType.wrap(
  0x737900000000000000000000000000004379636c654e6f6e636f6d626174536b
);

struct CallWrapper {
  ResourceId systemId;
  address from;
}

struct RootCallWrapper {
  ResourceId systemId;
  address from;
}

/**
 * @title CycleNoncombatSkillSystemLib
 * @author MUD (https://mud.dev) by Lattice (https://lattice.xyz)
 * @dev This library is automatically generated from the corresponding system contract. Do not edit manually.
 */
library CycleNoncombatSkillSystemLib {
  error CycleNoncombatSkillSystemLib_CallingFromRootSystem();

  function castNoncombatSkill(CycleNoncombatSkillSystemType self, bytes32 cycleEntity, bytes32 skillEntity) internal {
    return CallWrapper(self.toResourceId(), address(0)).castNoncombatSkill(cycleEntity, skillEntity);
  }

  function castNoncombatSkill(CallWrapper memory self, bytes32 cycleEntity, bytes32 skillEntity) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert CycleNoncombatSkillSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _castNoncombatSkill_bytes32_bytes32.castNoncombatSkill,
      (cycleEntity, skillEntity)
    );
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function castNoncombatSkill(RootCallWrapper memory self, bytes32 cycleEntity, bytes32 skillEntity) internal {
    bytes memory systemCall = abi.encodeCall(
      _castNoncombatSkill_bytes32_bytes32.castNoncombatSkill,
      (cycleEntity, skillEntity)
    );
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function callFrom(CycleNoncombatSkillSystemType self, address from) internal pure returns (CallWrapper memory) {
    return CallWrapper(self.toResourceId(), from);
  }

  function callAsRoot(CycleNoncombatSkillSystemType self) internal view returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), WorldContextConsumerLib._msgSender());
  }

  function callAsRootFrom(
    CycleNoncombatSkillSystemType self,
    address from
  ) internal pure returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), from);
  }

  function toResourceId(CycleNoncombatSkillSystemType self) internal pure returns (ResourceId) {
    return ResourceId.wrap(CycleNoncombatSkillSystemType.unwrap(self));
  }

  function fromResourceId(ResourceId resourceId) internal pure returns (CycleNoncombatSkillSystemType) {
    return CycleNoncombatSkillSystemType.wrap(resourceId.unwrap());
  }

  function getAddress(CycleNoncombatSkillSystemType self) internal view returns (address) {
    return Systems.getSystem(self.toResourceId());
  }

  function _world() private view returns (IWorldCall) {
    return IWorldCall(StoreSwitch.getStoreAddress());
  }
}

/**
 * System Function Interfaces
 *
 * We generate an interface for each system function, which is then used for encoding system calls.
 * This is necessary to handle function overloading correctly (which abi.encodeCall cannot).
 *
 * Each interface is uniquely named based on the function name and parameters to prevent collisions.
 */

interface _castNoncombatSkill_bytes32_bytes32 {
  function castNoncombatSkill(bytes32 cycleEntity, bytes32 skillEntity) external;
}

using CycleNoncombatSkillSystemLib for CycleNoncombatSkillSystemType global;
using CycleNoncombatSkillSystemLib for CallWrapper global;
using CycleNoncombatSkillSystemLib for RootCallWrapper global;

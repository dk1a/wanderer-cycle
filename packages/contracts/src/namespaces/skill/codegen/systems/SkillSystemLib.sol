// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

import { SkillSystem } from "../../SkillSystem.sol";
import { revertWithBytes } from "@latticexyz/world/src/revertWithBytes.sol";
import { IWorldCall } from "@latticexyz/world/src/IWorldKernel.sol";
import { SystemCall } from "@latticexyz/world/src/SystemCall.sol";
import { WorldContextConsumerLib } from "@latticexyz/world/src/WorldContext.sol";
import { Systems } from "@latticexyz/world/src/codegen/tables/Systems.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";

type SkillSystemType is bytes32;

// equivalent to WorldResourceIdLib.encode({ typeId: RESOURCE_SYSTEM, namespace: "skill", name: "SkillSystem" }))
SkillSystemType constant skillSystem = SkillSystemType.wrap(
  0x7379736b696c6c000000000000000000536b696c6c53797374656d0000000000
);

struct CallWrapper {
  ResourceId systemId;
  address from;
}

struct RootCallWrapper {
  ResourceId systemId;
  address from;
}

/**
 * @title SkillSystemLib
 * @author MUD (https://mud.dev) by Lattice (https://lattice.xyz)
 * @dev This library is automatically generated from the corresponding system contract. Do not edit manually.
 */
library SkillSystemLib {
  error SkillSystemLib_CallingFromRootSystem();
  error SkillSystem_SkillMustBeLearned(bytes32 userEntity, bytes32 skillEntity);
  error SkillSystem_SkillOnCooldown(bytes32 userEntity, bytes32 skillEntity);
  error SkillSystem_NotEnoughMana(uint32 cost, uint32 current);

  function useSkill(SkillSystemType self, bytes32 userEntity, bytes32 skillEntity, bytes32 targetEntity) internal {
    return CallWrapper(self.toResourceId(), address(0)).useSkill(userEntity, skillEntity, targetEntity);
  }

  function useSkill(CallWrapper memory self, bytes32 userEntity, bytes32 skillEntity, bytes32 targetEntity) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert SkillSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _useSkill_bytes32_bytes32_bytes32.useSkill,
      (userEntity, skillEntity, targetEntity)
    );
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function useSkill(
    RootCallWrapper memory self,
    bytes32 userEntity,
    bytes32 skillEntity,
    bytes32 targetEntity
  ) internal {
    bytes memory systemCall = abi.encodeCall(
      _useSkill_bytes32_bytes32_bytes32.useSkill,
      (userEntity, skillEntity, targetEntity)
    );
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function callFrom(SkillSystemType self, address from) internal pure returns (CallWrapper memory) {
    return CallWrapper(self.toResourceId(), from);
  }

  function callAsRoot(SkillSystemType self) internal view returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), WorldContextConsumerLib._msgSender());
  }

  function callAsRootFrom(SkillSystemType self, address from) internal pure returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), from);
  }

  function toResourceId(SkillSystemType self) internal pure returns (ResourceId) {
    return ResourceId.wrap(SkillSystemType.unwrap(self));
  }

  function fromResourceId(ResourceId resourceId) internal pure returns (SkillSystemType) {
    return SkillSystemType.wrap(resourceId.unwrap());
  }

  function getAddress(SkillSystemType self) internal view returns (address) {
    return Systems.getSystem(self.toResourceId());
  }

  function _world() private view returns (IWorldCall) {
    return IWorldCall(StoreSwitch.getStoreAddress());
  }
}

/**
 * System Function Interfaces
 *
 * We generate an interface for each system function, which is then used for encoding system calls.
 * This is necessary to handle function overloading correctly (which abi.encodeCall cannot).
 *
 * Each interface is uniquely named based on the function name and parameters to prevent collisions.
 */

interface _useSkill_bytes32_bytes32_bytes32 {
  function useSkill(bytes32 userEntity, bytes32 skillEntity, bytes32 targetEntity) external;
}

using SkillSystemLib for SkillSystemType global;
using SkillSystemLib for CallWrapper global;
using SkillSystemLib for RootCallWrapper global;
